on:
  workflow_dispatch:
  push:
    
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: 'recursive'
    - uses: docker/build-push-action@v5
      with:
        context: symcc
